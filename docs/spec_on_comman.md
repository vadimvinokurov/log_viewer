# 📄 Функциональная спецификация: Log Viewer v2.0
## ✅ Финальная версия (после интервью)

---

## 1. Общее описание продукта

### 1.1. Назначение
Приложение для просмотра, фильтрации и анализа лог-файлов с Vim-подобным интерфейсом: модальный подход, командная строка с префиксом `:`, навигация в стиле Vim.

---

## 2. Функциональные требования

### 2.1. Типы поиска/фильтрации

**Важно:** Для plain и simple режимов вводимый текст НЕ требует экранирования. Для regex режима используется стандартный синтаксис регулярных выражений.

**По умолчанию:** все команды поиска, фильтров и хайлайтов работают в режиме case insensitive (игнорируют регистр). Для учёта регистра используйте флаг `/cs`.

| Тип | Команда | Пример | Описание |
|-----|---------|--------|----------|
| **Plain** | `:s`, `:f`, `:h` | `:f Some text` | Поиск подстроки |
| **Regex** | `:sr`, `:fr`, `:hr` | `:fr error_\d+` | Регулярное выражение |
| **Simple** | `:ss`, `:fs`, `:hs` | `:fs "ERROR" AND "timeout"` | Булева логика |

**Флаги (добавляются через `/`):**

| Флаг | Описание |
|------|----------|
| `/cs` | Case sensitive — учитывать регистр |
| `/color=<color>` | Цвет для хайлайта |

**Формат команды:**

```
:команда текст                           # без флагов (пробел — разделитель)
:команда/флаги/текст                     # с флагами (слеш после флагов)
```

**Примеры:**

```
:f Some text                    # без флагов, ищем "Some text"
:f/cs/Some text                 # с флагом cs, ищем "Some text"
:fs "ERROR" AND "timeout"       # case insensitive, simple
:h/color=red/Some text          # case insensitive, цвет red
:h/cs/color=yellow/Some text    # case sensitive, цвет yellow
:hr/error_\d+                   # case insensitive, regex
```
**Антипример:**
```
:f//path/to/file               # пустых флагов быть не может - ошибка
```

**Примечание:** Пустых флагов НЕ ДОЛЖНО быть: f//path/to/file - ошибка

**Примечание:** Первый пробел после команды — разделитель. Если текст начинается с пробела, используйте второй пробел: `:f  text` (ищет " text" с ведущим пробелом).

#### Simple mode синтаксис:

```
Операторы: AND, OR, NOT (case-insensitive)
Группировка: ( )
Текст: "двойные кавычки"
Приоритет: NOT > AND > OR (как в булевой алгебре)
```

**Примеры Simple mode:**
```
:ss "ERROR"
:ss "ERROR" OR "WARNING"
:ss "ERROR" AND "timeout"
:ss "ERROR" AND ("timeout" OR "connection") NOT "debug"
```

---

### 2.2. Режимы работы

**Модальный подход (как в Vim):**

| Мода | Активация | Назначение |
|------|-----------|------------|
| **Normal mode** | По умолчанию | Навигация по логам |
| **Command mode** | `:` | Ввод команд |
| **Search mode** | `/` или `?` | Поиск вперёд/назад |

**Три режима работы с паттернами:**

| Режим | Команда | Поведение |
|-------|---------|-----------|
| **Search** | `:s`, `:sr`, `:ss`| Навигация к совпадениям |
| **Filter** | `:f`, `:fr`, `:fs` | Отображение только совпадающих строк |
| **Highlight** | `:h`, `:hr`, `:hs` | Подсветка совпадений цветом |

**Поиск через `/` и `?`:** работает как же как `:s`, это считай alias для `:s`. 

**Навигация по совпадениям:**

| Клавиша | Действие |
|---------|----------|
| `n` | Следующее совпадение |
| `N` | Предыдущее совпадение |

**Комбинирование:**
- Несколько **фильтров** комбинируются через **OR** (строка показывается если попадает хоть в один фильтр)
- Несколько **хайлайтов** отображаются одновременно разными цветами
- **Search** работает в рамках текущего фильтра и категорий

### 2.3. Навигация и команды

#### 2.3.1. Навигация (Normal mode)

| Клавиша | Действие |
|---------|----------|
| `j` | Вниз на одну строку |
| `k` | Вверх на одну строку |
| `gg` | В начало файла |
| `G` | В конец файла |
| `{N}G` | Перейти на строку N (например, `42G` — строка 42) |
| `Ctrl+D` | Полстраницы вниз |
| `Ctrl+U` | Полстраницы вверх |
| `yy` | Скопировать текущую строку в системный буфер обмена |
| `Esc` | Отмена / возврат в Normal mode |

**Поиск:**

| Клавиша | Действие |
|---------|----------|
| `/text` | Поиск вперёд (работае так же как и `:s`) |
| `?text` | Поиск назад (работае так же как и `:s`) |
| `n` | Следующее совпадение |
| `N` | Предыдущее совпадение |

#### 2.3.2. Команды (Command mode `:`)

**Файловые операции:**

| Команда | Действие |
|---------|----------|
| `:open <path>` | Открыть файл или HTTP URL (формат default) |
| `:reload` | Перезагрузить текущий файл (фильтры/хайлайты/категории сохраняются) |

**Поиск:**

| Команда | Действие |
|---------|----------|
| `:s <text>` | Поиск plain (case insensitive) |
| `:s/cs/<text>` | Поиск plain (case sensitive) |
| `:sr <pattern>` | Поиск regex (case insensitive) |
| `:sr/cs/<pattern>` | Поиск regex (case sensitive) |
| `:ss <text>` | Поиск simple (case insensitive) |
| `:ss/cs/<text>` | Поиск simple (case sensitive) |

**Фильтры:**

| Команда | Действие |
|---------|----------|
| `:f <text>` | Фильтр plain (case insensitive) |
| `:f/cs/<text>` | Фильтр plain (case sensitive) |
| `:fr <pattern>` | Фильтр regex (case insensitive) |
| `:fr/cs/<pattern>` | Фильтр regex (case sensitive) |
| `:fs <text>` | Фильтр simple (case insensitive) |
| `:fs/cs/<text>` | Фильтр simple (case sensitive) |
| `:rmf <text>` | Удалить фильтр (по точному совпадению) |
| `:rmf` | Очистить все фильтры |
| `:lsf` | Показать активные фильтры |

**Highlights:**

| Команда | Действие |
|---------|----------|
| `:h <text>` | Хайлайт plain (case insensitive, дефолтный цвет) |
| `:h/cs/<text>` | Хайлайт plain (case sensitive, дефолтный цвет) |
| `:h/color=red/<text>` | Хайлайт plain (case insensitive, цвет red) |
| `:h/cs/color=red/<text>` | Хайлайт plain (case sensitive, цвет red) |
| `:hr <pattern>` | Хайлайт regex (case insensitive, дефолтный цвет) |
| `:hr/color=yellow/<pattern>` | Хайлайт regex (case insensitive, цвет yellow) |
| `:hs <text>` | Хайлайт simple (case insensitive, дефолтный цвет) |
| `:rmh <text>` | Удалить хайлайт (по точному совпадению) |
| `:rmh` | Очистить все хайлайты |
| `:lsh` | Показать активные хайлайты |

**Категории:**

| Команда | Действие |
|---------|----------|
| `:cate <path>` | Включить категорию |
| `:catd <path>` | Выключить категорию |
| `:catea` | Включить все категории |
| `:catda` | Выключить все категории |
| `:lscat` | Показать дерево категорий |

**Пресеты:**

| Команда | Действие |
|---------|----------|
| `:preset <name>` | Сохранить пресет |
| `:presetl <name>` | Загрузить пресет (ошибка если не найден) |
| `:rmpreset <name>` | Удалить пресет |
| `:lspreset` | Показать список пресетов |

**Другое:**

| Команда | Действие |
|---------|----------|
| `:theme <name>` | Сменить тему (dark/light) |
| `:n` | Следующее совпадение поиска |
| `:N` | Предыдущее совпадение поиска |
| `:q` | Выход |

#### 2.3.3. Флаги

**Доступные флаги:**

| Флаг | Описание |
|------|----------|
| `/cs` | Case sensitive — учитывать регистр |
| `/color=<color>` | Цвет для хайлайта |

**Формат для команд :s,:sr,:sf,:f,:fr,:fs,:h,:hr,:hs:**
```
:команда <текст>                    # без флагов
:команда/флаги/<текст>               # с флагами
```

**Примеры:**
```
:f Some text                    # case insensitive, plain
:f/cs/Some text                 # case sensitive, plain
:h/color=red/Some text          # case insensitive, цвет red
:h/cs/color=yellow/Some text   # case sensitive, цвет yellow
```

**Дефолтный цвет для хайлайтов:** первый цвет из списка (red).

**Именованные цвета (базовые):**
- `red`, `green`, `blue`, `yellow`, `cyan`, `magenta`, `white`, `black`, `orange`, `purple`, `pink`, `brown`, `grey`

**Кастомные цвета:**
- HEX: `/color=#FF5733`

#### 2.3.4. Удаление фильтров/хайлайтов

Удаление по полному соответствию команды (включая флаги).

**Примеры:**
```
# Добавление                → Удаление
:f Some text                → :rmf Some text                ✅
:f/cs/Some text             → :rmf/cs/Some text             ✅
:h Some text                → :rmh Some text                ✅
:h/color=red/Some text      → :rmh/color=red/Some text     ✅
```

#### 2.3.6. История команд

**Навигация (`↑`/`↓` в Command mode):**
- Прокрутка истории команд
- Не последовательные дубликаты сохраняются

#### 2.3.7. Просмотр активных фильтров и хайлайтов

```
:lsf                     # Список фильтров
:lsh                     # Список хайлайтов
```

**Формат вывода `:lsf`:**
```
Active filters (2):
  1. :f Some text
  2. :f/cs/Some text 
```

**Формат вывода `:lsh`:**
```
Active highlights (2):
  1. :h Some text
  2. :h/cs/Some text 
```

---

### 2.4. Система категорий

#### 2.4.1. Формат строки лога
```
<timestamp> <category> <level> <message>
```

**Пример:**
```
2026-03-20T12:20:42.305 network/game_storage/folder LOG_WARNING Unable to find label.
```

| Поле | Позиция | Формат | Обязательно |
|------|---------|--------|-------------|
| Timestamp | 0 | ISO8601 (без timezone) | ✅ Да |
| Category | 1 | `path/like/this` | ✅ Да |
| Level | 2 | `LOG_*` (напр. `LOG_ERROR`) | ✅ Да |
| Message | 3+ | Любой текст | ✅ Да |

**Примечание:** Строки без поля LOG_* уровня по умолчанию причисляются к LOG_INFO. Строки с менее чем 2 полями (timestamp + хотя бы одно поле) попадают в категорию «uncategorized» с уровнем LOG_INFO.

#### 2.4.2. Принцип работы категорий

| Правило | Описание |
|---------|----------|
| **Наследование вниз** | При `:cate` включается категория и все дочерние |
| **Наследование вниз** | При `:catd` выключается категория и все дочерние |
| **Приоритет листа (Leaf Priority)** | Явно заданная дочерняя категория переопределяет родителя |
| **По умолчанию** | Все категории включены |

#### 2.4.3. Пример наследования

**Исходные данные:**
```
2026-03-20T12:20:42.305 network Message 1
2026-03-20T12:20:42.305 network/dns Message 2
2026-03-20T12:20:42.305 network/dns/socket Message 3
```

**Команды:**
```
:cate network
:catd network/dns
```

**Результат в панели логов:**
```
2026-03-20T12:20:42.305 network Message 1
```
*(Message 2 и Message 3 скрыты, так как `network/dns` выключена явно)*

#### 2.4.4. Отображение дерева категорий

```
┌─────────────────────────────────────────────────────────────┐
│  Categories                                                 │
├─────────────────────────────────────────────────────────────┤
│  ✅ application                                             │
│  ✅   auth                                                  │
│  ✅   api                                                   │
│  ✅ network                                                 │
│  ❌   dns                                                   │
│  ✅     socket                                              │
└─────────────────────────────────────────────────────────────┘
```

---

### 2.5. Система пресетов

#### 2.5.1. Хранение
- **Локация:** `~/.logviewer/presets/<имя>.yaml`
- **Формат:** YAML
- **Тип:** Глобальные (не привязаны к файлу)

#### 2.5.2. Структура пресета
```yaml
name: my-errors
filters:
  - :fs "ERROR" AND "timeout"
  - :fr/error_\d+
highlights:
  - :h/color=red/ERROR
  - :h/color=yellow/WARNING
disabledCategories:
  - network/dns
  - application/debug
```

#### 2.5.3. Применение пресета

**Partial apply:** пресет изменяет только указанные категории и фильтры/хайлайты.

| Правило | Поведение |
|---------|-----------|
| **Категории** | Применяются только указанные в `disabledCategories` |
| **Неуказанные категории** | Состояние не меняется |
| **Несуществующие категории** | Игнорируются (без ошибки) |
| **Несуществующий пресет** | Ошибка: `❌ Preset 'name' not found` |
| **Фильтры/хайлайты** | Добавляются к текущим, дубликаты игнорируются |

**Пример:**
```
Текущее состояние: network/dns = enabled, network/transport = enabled

Пресет (my-errors.yaml):
disabledCategories:
  - network/dns

После применения: network/dns = disabled, network/transport = enabled (не изменился)
```

---

### 2.6. Управление файлами

| Функция | Описание |
|---------|----------|
| **Локальные файлы** | Открытие файлов по абсолютному/относительному пути |
| **HTTP источники** | Открытие логов по HTTP URL (GET запрос, без аутентификации). При ошибке — сообщение в статус баре: `❌ HTTP 404: Not Found` |
| **Загрузка в память** | Все строки загружаются в RAM (без лимита в v2.0) |
| **Перезагрузка** | `:reload` — перечитать текущий файл (фильтры/хайлайты/категории сохраняются) |
| **Один файл** | `:open` закрывает текущий файл, открывает новый |

---

## 3. Пользовательский интерфейс

### 3.1. Копирование в буфер

| Действие | Поведение |
|----------|-----------|
| **Ctrl+C / Cmd+C** | Копировать выделенные строки |
| **Без выделения** | Копировать текущую строку под курсором |
| **Формат** | Оригинальные строки из файла (с сохранением форматирования) |

---

### 3.1. Консольный UI (TUI)

#### 3.1.1. Layout экрана

Двухпанельный интерфейс с категориями справа.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  LogViewer v2.0                                              [dark theme]  │
├────────────────────────────────────────────────┬────────────────────────────┤
│  Line │ Timestamp     │ Level │ Category    │ M│  Categories         [±]    │
│───────┼───────────────┼───────┼─────────────┼──┤  ▶ ✅ application (1,200)   │
│  1    │ 2026-03-23... │ ERROR │ network     │ T│  ▶ ✅ network (500)       │
│  2    │ 2026-03-23... │ INFO  │ database    │ P│  ▶ ❌ database (300)        │
│  3    │ 2026-03-23... │ WARN  │ application │ H│  ⚠️ uncategorized (127)    │
│  ...  │ ...           │ ...   │ ...         │..│                            │
├────────────────────────────────────────────────┴────────────────────────────┤
│  > _                        │ app.log: 1,287/15,432 │ ⛔1 🛑2 ⚠️3 ℹ️4 🟪5 🟩6 │
└─────────────────────────────────────────────────────────────────────────────┘
```

**Панели:**

| Панель | Расположение | Содержимое |
|--------|--------------|------------|
| **Log Panel** | Левая, основная | Таблица логов с колонками |
| **Categories Panel** | Правая, настраиваемая ширина | Дерево категорий |
| **Status Bar** | Нижняя строка | Командная строка + статистика |

**Категории Panel:**
- По умолчанию 25% ширины
- Настраиваемая через drag границы
- Кнопки `[+]` (Expand All) и `[-]` (Collapse All) в заголовке

#### 3.1.2. Категории Panel

**Отображение:**
- Дерево со сворачиванием (collapse)
- При старте — все категории свёрнуты
- `▶` — свёрнутый узел, `▼` — развёрнутый
- `✅` — включена, `❌` — выключена, `⚠️` — uncategorized

**Взаимодействие (мышь):**
| Действие | Поведение |
|----------|-----------|
| Клик по `▶`/`▼` | Развернуть/свернуть узел |
| Клик по названию | Toggle категории (вкл/выкл) |
| Клик по `[+]` | Развернуть все |
| Клик по `[-]` | Свернуть все |
| Drag границы | Изменить ширину панели |

#### 3.1.3. Log Panel

**Колонки (фиксированные):**

| Колонка | Содержимое | Ширина |
|---------|------------|--------|
| Line | Номер строки | По содержимому |
| Timestamp | Временная метка | По содержимому |
| Level | Уровень лога | По содержимому |
| Category | Категория | По содержимому |
| Message | Текст сообщения | Оставшаяся ширина |

**Ширина колонок:**
- Изначально — по содержимому
- Настраиваемая через drag границ колонок

**Виртуальная прокрутка:**
- Рендерит только видимые строки
- Поддерживает файлы любого размера
- Плавный скролл

**Выделение:**
| Действие | Поведение |
|----------|-----------|
| Клик по строке | Выделить строку |
| `Ctrl/Cmd + клик` | Добавить в выделение |

#### 3.1.4. Status Bar

Одна строка внизу экрана, разделённая на две части.

```
│  > _                         │ app.log: 1,287/15,432 │ ⛔1 🛑2 ⚠️3 ℹ️4 🟪5 🟩6 │
```

| Часть | Расположение | Содержимое |
|-------|--------------|------------|
| **Командная строка** | Левая | `> _` с курсором |
| **Статистика** | Правая | Имя файла, счётчики уровней |

**Динамическое поведение:**
- При длинной команде статистика скрывается
- Вся строка отдаётся под команду
- При загрузке HTTP — прогресс бар, затем количество строк

**Прогресс загрузки (HTTP):**
```
│  > _                         │ Loading: ████████░░ 80% (12.4s)         │
```

**Статистика уровней:** Показываются все уровни, включая нулевые счётчики.

**Сообщения об ошибках:**
```
│  > _                         │ ❌ File not found: /var/log/app.log     │
```

---

### 3.2. Модель управления (Focus Model)

#### 3.2.1. Режимы ввода

| Режим | Активация | Поведение |
|-------|-----------|-----------|
| **Normal mode** | По умолчанию | Навигация по логам (`j`/`k`, `gg`/`G`, `/`, `?`, `n`/`N`) |
| **Command mode** | `:` | Ввод команды до Enter/Esc |
| **Search mode** | `/` или `?` | Ввод паттерна поиска до Enter/Esc |

#### 3.2.2. Переключение режимов

```
Нажатие `:` → Command mode (ввод команды)
Нажатие `/` → Search mode (поиск вперёд)
Нажатие `?` → Search mode (поиск назад)
Нажатие `Enter` → Выполнить → Normal mode
Нажатие `Esc` → Отменить → Normal mode
```

#### 3.2.3. Мышь (работает всегда)
| Действие | Поведение |
|----------|-----------|
| Клик по строке | Выделение строки |
| Scroll колёсиком | Прокрутка Log Panel |
| Клик по категории | Enable/disable категорию |
| Shift+клик | Выделение диапазона строк |
| Ctrl/Cmd+клик | Добавить в выделение |
| Drag границ панелей | Resize панелей |
| Drag границ колонок | Resize колонок в Log Panel |

---

### 3.3. Цветовые темы

#### 3.3.1. Dark theme (по умолчанию)

| Элемент | Цвет |
|---------|------|
| Фон | `#1E1E1E` |
| Текст | `#D4D4D4` |
| Выделение | `#264F78` |
| Акцент | `#569CD6` |

#### 3.3.2. Light theme

| Элемент | Цвет |
|---------|------|
| Фон | `#FFFFFF` |
| Текст | `#1E1E1E` |
| Выделение | `#C7DEF8` |
| Акцент | `#0066CC` |

#### 3.3.3. Подсветка уровней логирования

| Уровень | Цвет | Формат |
|---------|------|--------|
| CRITICAL | Ярко-красный | Инвертированный фон |
| ERROR | Красный | Цвет текста |
| WARNING | Жёлтый | Цвет текста |
| INFO | Стандартный | Цвет текста (тема) |
| DEBUG | Стандартный | Цвет текста (тема) |
| TRACE | Стандартный | Цвет текста (тема) |

#### 3.3.4. Переключение темы

```
:theme dark
:theme light
```

Тема сохраняется в `~/.logviewer/settings.json` между сессиями.

---

### 3.4. Навигация (клавиатура)

#### 3.4.1. Normal mode (по умолчанию)

| Клавиша | Действие |
|---------|----------|
| `j` | Вниз на одну строку |
| `k` | Вверх на одну строку |
| `↑`/`↓` | Вверх/Вниз на одну строку |
| `gg` | В начало файла |
| `G` | В конец файла |
| `{N}G` | Перейти на строку N (например, `42G` — строка 42) |
| `Ctrl+D` | Полстраницы вниз |
| `Ctrl+U` | Полстраницы вверх |
| `/pattern` | Поиск вперёд |
| `?pattern` | Поиск назад |
| `n` | Следующее совпадение |
| `N` | Предыдущее совпадение |
| `yy` | Скопировать текущую строку в системный буфер обмена |
| `:` | Переход в Command mode |
| `Esc` | Отмена |

#### 3.4.2. Command mode

| Клавиша | Действие |
|---------|----------|
| `Enter` | Выполнить команду |
| `Esc` | Отменить ввод |
| `↑`/`↓` | История команд |

#### 3.4.3. Search mode

| Клавиша | Действие |
|---------|----------|
| `Enter` | Начать поиск |
| `Esc` | Отменить поиск |

---

### 3.5. Обратная связь пользователю

| Событие | Индикация |
|---------|-----------|
| **Успешное действие** | Без сообщения (результат виден в UI) |
| **Ошибка команды** | Временное сообщение в статус баре (красный цвет) |
| **Загрузка файла** | Прогресс бар в статус баре |

**Выход из приложения:**
- `:q` — выход без подтверждения
- Несохранённые фильтры/хайлайты теряются

---

### 3.6. История команд

| Аспект | Поведение |
|--------|-----------|
| **Хранение** | `~/.logviewer/history.json` |
| **Размер** | 100 команд (настраивается в settings.json) |
| **Навигация** | `↑`/`↓` в Command mode |
| **Дубликаты** | Последовательные дубликаты не сохраняются |

---

### 3.7. Копирование в буфер

| Действие | Поведение |
|----------|-----------|
| `Ctrl+C` / `Cmd+C` | Копировать выделенные строки |
| Без выделения | Копировать текущую строку под курсором |
| **Формат** | Оригинальные строки из файла (с сохранением форматирования) |

---

## 4. Нефункциональные требования

### 4.1. Производительность
| Требование | Значение |
|------------|----------|
| Загрузка файла 1GB | < 10 сек |
| Применение фильтра | < 100 мс |
| Переключение темы | < 50 мс |
| Подсчёт статистики | Асинхронно, не блокирует UI |

### 4.2. Совместимость
| Платформа | Поддержка |
|-----------|-----------|
| **Linux** | ✅ Полная |
| **macOS** | ✅ Полная |
| **Windows** | ✅ Полная (PowerShell, CMD) |
| **Python** | 3.9+ |

### 4.3. Конфигурация
**Файл конфигурации:** `~/.logviewer/settings.json`

```json
{
  "theme": "dark",
  "historySize": 100,
  "defaultFormat": "default",
  "highlightColors": ["red", "yellow", "green", "cyan", "magenta", "blue", "white"],
  "defaultCategoriesEnabled": true
}
```

### 4.4. История команд
- **Хранение:** `~/.logviewer/history.json`
- **Размер:** Определяется `historySize` в settings.json (по умолчанию 100)
- **Доступ:** Только `↑` / `↓` в Command mode
- **Навигация по логам:** Отдельные клавиши (`↑`/`↓` в Navigation mode, `Page Up`/`Page Down`, `g`/`G`)

---

## 5. Глоссарий терминов

| Термин | Определение |
|--------|-------------|
| **Search** | Режим навигации по совпадениям без фильтрации |
| **Filter** | Режим отображения только совпадающих строк |
| **Highlight** | Режим подсветки совпадений цветом |
| **Simple mode** | Кастомный синтаксис с булевой логикой (AND, OR, NOT) |
| **Preset** | Сохранённая конфигурация (фильтры + хайлайты + категории) |
| **Category** | Иерархическая метка лога (например, `network/transport/link`) |
| **Leaf Priority** | Явно заданная дочерняя категория переопределяет родителя |
| **TUI** | Text User Interface (консольный интерфейс) |
| **Status bar** | Нижняя панель со статистикой |
| **Toast** | Всплывающее уведомление для ошибок/важных событий |
| **Normal mode** | Режим навигации по логам (по умолчанию) |
| **Command mode** | Режим ввода команд (после `:`) |
| **Search mode** | Режим поиска (после `/` или `?`) |
| **Unified Gateway** | Единая логика парсинга для Search/Filter/Highlight |